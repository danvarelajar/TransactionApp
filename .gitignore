# Dependencies
node_modules/
npm-debug.log
yarn-error.log

# Environment variables (contains sensitive config). Ignore all `.env*` files so patterns
# from a global gitignore cannot accidentally block only some of them—but always keep the
# committed template tracked:
.env*
!.env.example

# Logs
*.log
logs/

# Documentation files
*.md

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Docker
.dockerignore

# Build outputs
dist/
build/

# Setup scripts (local helper scripts - don't commit)
setup-hosts.sh
setup-hosts.ps1 